import styled from 'styled-components'

export const SpinnerPosition = styled.div`
  display: flex;
  justify-content: center;
  align-items: center;
  overflow: hidden;
  min-height: 30vh;

  svg {
    width: 60px;
    height: 60px;
  }
`
